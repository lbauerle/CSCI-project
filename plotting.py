import matplotlib.pyplot as plt
import numpy as np


def timecontourplot(timedata, axisdata, contourdata, axis, maxint, cmap, imagetype='png'):
    """
    
    """
    return figure


def twodimcontourplot(axis1data, axis2data, contourdata, axis1, axis2, maxint, cmap, imagetype='png'):
    """
    
    """
    return figure


def timelasercontourplot(timedata, axisdata, contourdata, laserdata, axis, maxint, cmap, imagetype='png'):
    """
    
    """
    return figure

def twodimlasercontourplot(axis1data, axis2data, contourdata, timedata, laserdata, axis1, axis2, maxint, cmap, imagetype='png'):
    """
    
    """
    return figure


def makegif(figurefolder):
    
    return gif